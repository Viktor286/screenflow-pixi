import { css } from 'emotion';

export const mainContainer = css`
  display: flex;
  width: auto;
  left: 50%;
  transform: translateX(-50%);
  margin: 10px auto 0;
  padding: 0;
  position: absolute;
  justify-content: center;
`;
